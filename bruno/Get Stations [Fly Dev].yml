info:
  name: Get Stations [Fly Dev]
  type: http
  seq: 3

http:
  method: GET
  url: https://toms-fuel-finder.fly.dev/stations?postcode=CM29JT&max_distance_miles=5
  params:
    - name: postcode
      value: CM29JT
      type: query
    - name: max_distance_miles
      value: "5"
      type: query
    - name: sort
      value: distance
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
